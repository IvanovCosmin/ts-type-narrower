{
  "name": "type-narrower-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript toolchain only used to typecheck the fixture project; the analyzer itself is Rust (see Cargo.toml).",
  "scripts": {
    "check:fixture": "tsc -p fixture/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
